
<%= simple_form_for(@wallpost, html: {class: "form-horizontal"}) do |f| %>
  <% if @wallpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wallpost.errors.count, "error") %> prohibited this wallpost from being saved:</h2>

      <ul>
      <% @wallpost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.input :user_id, collection: [User.find(current_user.id)], label_method: :profile_name, include_blank: false %>
    <%= f.input :content %>
 
  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>
 
   